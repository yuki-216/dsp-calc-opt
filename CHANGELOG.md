# 更新日志

## Unreleased - 2026-08-27

### 变更
- 移除 narrow/mobile 档位,手机强制桌面版:viewport 对齐电脑模式固定 ~980px(桌面版站点典型宽度),手机直接打开与开电脑模式一致(slender 档)且可自由缩放;断点收敛为 full/compact/semi/mid/slender 五档,<900px 兜底 slender;删除窄屏「整面板收纳」与移动端极简 CSS;`is_mobile`/`hideIntHint` 相关分支自动失效(整数建议不再悬浮、设置行4不再单独显示等)。

## 0.10.0 - 2026-08-27

### 新增
- 结果表「整数优化建议」列（熔炉/制造台/化工厂）：混合工厂等级凑出偶数设备台数——紧凑模式换高一级省占地、省料模式尽量用低级设备避免浪费，纯前端提示不改 LP 核心。
- 结果表联产物各自成行：同配方多产物中自身无净产量的联产物不再缺失，追加在表尾成行，具备完整交互控件与来源标注；新增「主配方优先」语义（缺口必须由选定主配方补足）。
- 轨道采集器重构：按官方自耗机制（净采=毛采×(1−工作能量/采集能量)）建模单采集器净产量；种子查看器新增轨道采集器面板（3 类气态行星参数、采集速度科技、自动应用气态星数据）。

### 修复
- 设备数/耗电/占地随需求集合漂移（多产物配方由 BFS 入图顺序决定折算系数）：改为按「完成一次反应时长×建筑吞吐倍率」计算，只依赖配方本身。
- 多产物配方产量显示折半、设备归属反直觉、联产物独立成行边界偏宽。
- 依赖图「仅需求模式」副产物孤立无依赖边；需求表「需求电力」展示口径与实际不符（改用总发电量口径）。

### 变更
- 核心计算重构为整网 LP 配平（HiGHS WASM）：根治循环组联产物归因错误（氢/精炼油），删除矩阵求逆与迭代过滤；引擎单次计算耗时稳定 70-80ms。
- 结果表 UI 大改：合并「整数建议+配方选取」为单列（列宽逐行测量）；精简模式按 7 档视口断点逐级递进——compact×按钮/短配方 → semi 设备列下拉 → mid 增产列下拉 → slender 整数建议悬浮+右列收纳 → narrow 左列收纳 → mobile 极简；产能/设备/增产模式列宽全局固定。
- 挖矿耗电简化：矿机统一为「挖矿机」，按「矿量×单位采集耗电」计电，原油萃取站固定单位耗电；参数设置面板三行重排；分馏塔带速 10 档下拉（含 2/3/4 倍堆叠、默认 7200/min），分馏塔设备数不计算。
- 收纳弹出面板按左右列分组（左=资源/右=建筑+目标值），内容横向排列；顶部工具栏三按钮与自动优化按钮文字随档位图标化；燃料选择移除「精炼油」。
- mobile 种子查看页精简：隐藏星球树/天体详情/致谢，轨道采集器面板移到原致谢位置。

### 移除
- 「速率单位」分钟/秒切换控件（全局固定按分钟计算）；vite legacy 构建。

## 0.9.11 - 2026-08-22

- 修复共生产品代表选择（如原油精炼的氢/精炼油）：检测含共生产品对的 SCC 后，对每个候选把联产品当作原矿整线重算，测量联产品余量；联产品仍紧缺的候选判为无效，只在有效候选中取最小多余，从而正确选出紧缺品作代表、富余品作副产物被循环消耗。
- 引擎计算日志（共生产品机制等）并入统一的 `DEBUG` 控制台开关（`window.__DEBUG.on/off/toggle/status`），不再单独占用 UI 开关；日志仅显示在面板，不污染核心计算控制台输出。
- 清理遗留死数据 `cost_weight`（含物品额外成本每物品 4 字段）：从未被引擎/优化器读取、仅为重构前源项目占位；已从方案初始化移除，并在加载存档时自动清除，减小 localStorage 体积。

## 0.9.10 - 2026-08-21

- 结果区新增「其余目标值」板块：展示当前珍稀权重值、净热值（GJ），均带前后增减。
- 预估占地、多余产物增加增减展示（占地增减改为数值下方上下排布）。

## 0.9.9 - 2026-08-21

- 新增「增产剂自由等级」开关：自动优化时，生产增产剂 Mk.I/II/III 的配方可自由选择各级增产剂（≤ 最高等级），不受可选增产剂限制。
- 硅石默认配方改为直接获取（采矿），而非石矿配方。
- 测试文件集中到 `tests/` 目录，新增 `npm test` 脚本。

## 0.9.8 - 2026-08-21

- 新增「珍稀权重」优化策略，替代并移除「最小原矿瓶颈」；新增珍稀矿实用性修正（默认开启），刺笋结晶/金伯利矿石/分形硅石按可替代普通矿折算稀缺度（替代比例 95%）。
- 布局调整：可选增产剂行移到矿物可用量与添加需求之间，策略/自动优化按钮/可选增产剂行序重排。
- 默认深色主题、默认优化策略改为珍稀权重、无增产剂加权默认 0.1%。
- 燃料选择移除增产剂 Mk.I/II/III。
- 矿物可用量按钮在展开时高亮；切到非珍稀权重策略自动收回矿物可用量。
- 「已自动应用统计均值」提示仅在真正应用后显示 5 秒。
- 导航栏新增 star 文案链接。
- 全项目 eslint 清理，修复 hooks 依赖等代码问题。

## 0.9.7 - 2026-08-20

- 自动优化默认仅启用增产剂 Mk.III。
- 无增产剂加权默认值设为 0.5%。
- 同步优化器默认配置和相关文档。

## 0.9.6 - 2026-08-20

- 新增浏览器 WASM 种子查询，公开 GitHub Pages 版本默认无需 Python 后端。
- 保留本地 FastAPI 查询和统计后端，可通过控制台切换 `browser`、`backend` 或 `auto` 模式。
- 统计结果支持从公开 `public/stats.json` 读取；后端统计控制 UI 默认隐藏，可通过 `showStatsControls()` 显示。
- 内置 `dsp_search_seed` C++/Python 查询依赖，消除对项目外部绝对路径的依赖。
- 新增 WASM 构建脚本 `npm run build:wasm`，通过 `GLM_ROOT` 配置 GLM 路径。
- 统计计算使用 Welford online algorithm，维护方差、置信区间和相对误差，并以星区汇总指标判断收敛。
- 种子查看器支持 32–64 恒星数量、矿量/矿点应用，以及选择星区或指定恒星应用资源数据。
- 删除过时的 `docs/deploy.md`，并按当前实现重写 `docs/code-analysis.md`。
