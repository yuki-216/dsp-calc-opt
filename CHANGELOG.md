# 更新日志

## Unreleased - 2026-08-25

### Added
- 结果表新增「整数优化建议」列(仅熔炉/制造台/化工厂):纯前端后处理,不改 LP 核心——紧凑模式台数向下取整到偶数、只换高一级设备(省占地),省料模式向上取整到最小偶数、尽量用低级设备(避免浪费);最低级强制紧凑、最高级强制省料、中间等级按设置双档(紧凑/省料,默认紧凑);原浮点台数列保留,新列按优化类型着色(紧凑=橙·对应加速/省料=蓝·对应增产),悬停显示类型说明;向下取整不可行时最低级强制紧凑不提示、中间级紧凑自动回退省料方案;设置内两档单选样式对齐增产/加速。
- 结果表联产物各自成行:同配方多产物中自身无净产量的联产物(如等离子精炼联产的氢)此前完全不出现在结果表中,现在统一追加在表尾成行,具备完整交互控件(视为原矿/新窗口/配方选取/增产设置);产能列主数字为自产净量(纯联产物为 0.00),下方来源括号显示联产注入量与来源物品;工厂列无独立设备数时显示 0.00。
- 「主配方优先」语义(z-分摊约束):物品有净需求且用户选定了主配方时,缺口必须由该配方补足,LP 不许通过扩大其他配方执行次数来多产其联产供给;联产物被动抵消耗、被动填需求、多余作盈余展示的行为不变。
- 轨道采集器重构:数据流改为"前端只读单采集器实际产量"——核心按 3 个单采集器净产量(/min,默认 氢234.26/重氢43.79/可燃冰76.11)折算采集器产出,建模官方自耗机制(净采=毛采×(1−工作能量30MW/采集能量),能量 氢/重氢9MJ、可燃冰4.8MJ);设置页移除巨星三面板。种子查看器右侧新增轨道采集器面板(星球详情下方,两块独立):3类气态行星类型切换,当前类型 2 个原始速率图标输入(2位小数,点开显完整数字,失焦近似),采集速度科技%·10%步进,编辑/选行星即自动计算 2 个新单采集器产量并应用到前端(第3个清零),真实种子气态星下拉选中自动应用 gas_veins;星球树恒星右侧显示气态行星类型标签(气态巨星/高产气巨/冰巨星)。
- 结果表建筑统计中轨道采集器不再统计汇总台数,显示 ×? 并悬浮"单采集器产量/min",点击跳转种子查看器;建筑统计含采集器时,"无增产剂加权"右侧显示"去获取轨道采集器精确值"链接(10秒自动消失)。

### Fixed
- 设备数/耗电/占地随需求集合漂移(用户实测:全无增产下需求 60 塑料=2 个原油精炼厂,改成 60 塑料+60 氢=4 个厂,而 LP 解与原油输入完全相同):设备数公式从旧引擎按"锚定物品净产出速率"折算移植,多产物配方(等离子精炼等 4 条)的折算系数取决于 BFS 由哪个物品先触发入图;改为按"完成一次反应的时长×建筑吞吐倍率"计算,只依赖配方本身。附带修正:原版口径下 120 精炼油/min 本需 4 厂(Speed=1、反应 4s→30 油/min/厂,旧显示 2 厂为少算一半);增产模式(增产效果)此前被重复折扣设备数,一并修复。
- 结果表多产物配方的产量显示折半(如需求 60/min 石墨烯只显示 30、联产氢来源括号显示 15):引擎新增 productionByItem(主物品净产量)供展示层使用,执行次数与产量口径分离。
- 多产物配方设备归属反直觉(如 氢、精炼油 都选 等离子精炼 时,氢 多余却持有设备、精炼油被当副产物):原 mainItem 由 BFS 入图顺序决定,设备/产量/联产来源随需求顺序漂移;改为结果映射层按"未多余(实际消耗)产物"归属展示主物品(平局取净产出最大,候选被其他配方占主物品时回退),LP 与计算不变。
- 联产物独立成行边界偏宽:纯多余联产物(本次计算从未被需求——既非顶层需求、也不被任何已入图配方消耗)此前也会被追加独立行;改为展示层用引擎返回的 graph(demandByItem ∪ 各配方原料)判断"被需求过",纯多余联产物不成行,多余量仍在"多余产物"面板展示。
- 依赖图「仅需求模式」副产物变孤立节点、无依赖边:无环投影原先只对配方主产物建"主→原料"边,副产物被忽略;改为对配方的每个产物(含副产物)都建"产物→真实原料"普通依赖边,与全部配方模式一致——副产物不再孤立,且能追溯到原配方原料链。
- 需求表「需求电力」展示与实际不符(输入 60 只显示设备自耗 4.16MW、电厂 1.93 台):引擎新增 totalPowerDemand(总发电量=净输出需求+设备自耗),置顶电力行/预估电力(MW)面板/发电设备数统一改基于总发电量——输入 60MW 时显示总发电 64.16MW、火电 29.7 台(64.16/2.16),与配比完全自洽;LP 守恒解(净输出 60)本就正确,仅聚合展示口径错误。

### Changed
- 核心计算重构为整网 LP 配平(HiGHS WASM):根治循环组联产物归因错误(氢/精炼油问题),删除矩阵求逆、阶段2逆生产、共生产品代表选择与过滤迭代。
- calculate() 变为异步;优化器与界面适配。
- 电力记账合并:生产/挖矿双轨合并为单一电力流,新增"不计挖矿电力"开关。
- 设置页移除「巨星氢/重氢/可燃冰面板」三参数(挪到轨道采集器面板的 3 接口);采集器速度倍率改用 gas_collect_speed(面板采集速度),不再受 mining_speed_multiple(采矿速度)影响。
- 挖矿耗电简化:采矿机/大型采矿机统一为「挖矿机」(设备数不计算,×?),不再按设备/矿脉数计电,改为「矿量 × 单位采集耗电」——新增滑块(0%=采矿机 2.333 → 100%=大型采矿机 9.19 kW/个,线性)÷采集速度;原油萃取站同样不算设备数,单位耗电固定 5.6 kW/个 ÷ 采集速度(油井速度固定 2.5/s);移除 小/大矿机覆盖矿脉数、大矿机开采速度、原油面板、采矿速度 设置(采集速度科技统一为 gas_collect_speed,与轨道采集器面板同步);抽水站保留设备+电力(基础产量固定 50/min);批量预设/结果表设备选择矿机合并为挖矿机;建筑统计不再统计挖矿机/原油萃取站,设备列 ×? 悬浮"设备数不计算"说明;移除「原矿显示」开关,改为自动隐藏"无多配方选择 且 无设备计算"的矿物行(铁矿/铜矿/石矿/煤矿/钛石/原油等,交互无意义,信息已充分显示在原矿需求表)。
- 依赖图浅层化:不再显示电力/增产剂依赖与循环组包围盒。
- 依赖图「仅需求模式」自建无环投影:不再复用核心引擎含电力/增产剂消耗边的 edges,改为从引擎二部图 recipes 用原始配方表过滤真实原料边——去掉 物品→电力 耗电边与 物品→喷涂增产剂 消耗边,保留增产剂配方内部真实链边(增产剂 Mk.III→Mk.II→Mk.I)与燃料链(电力→燃料);电力与增产剂作为独立需求节点显示(含无燃料时的孤立电力节点)。核心引擎与优化器(真实有环 SCC 分组)零改动。
- 移除 vite legacy 构建(structuredClone 已要求 Chrome 98+)。
- 引擎单次计算耗时约 70-80 ms(真实方案,不含首次 HiGHS 初始化),稳定且无病态慢路径(旧算法病态场景可达数百 ms 且结果错误)。
- 设置移除「速率单位」分钟/秒切换控件,全局固定按分钟计算;is_time_unit_minute 变常量 true(合并旧存档后强制覆盖,防 localStorage 残留 false 复活),分馏带速等依赖换算恒走按分钟分支。
- 参数设置面板 UI 重排为三行紧凑流式:第一行 精度位数(0-2)/研究站层数(3-15)/增产剂自喷涂/限制加速模式(加悬浮提示),第二行 挖矿单位耗电(滑块悬浮"最左挖矿机6矿脉/最右大矿机16矿脉3倍速")/采集速度(3位宽、百分号贴紧)/不计挖矿电力,第三行 分馏传送带(传送带360/高速720/极速1800 图标按钮,数值传后端 fractionating_speed=带速/60)/中间设备整数建议;顶部按钮「采矿参数 & 其他设置」改名「参数设置」;批量预设移除矿机组选择。

## 0.9.11 - 2026-08-22

- 修复共生产品代表选择（如原油精炼的氢/精炼油）：检测含共生产品对的 SCC 后，对每个候选把联产品当作原矿整线重算，测量联产品余量；联产品仍紧缺的候选判为无效，只在有效候选中取最小多余，从而正确选出紧缺品作代表、富余品作副产物被循环消耗。
- 引擎计算日志（共生产品机制等）并入统一的 `DEBUG` 控制台开关（`window.__DEBUG.on/off/toggle/status`），不再单独占用 UI 开关；日志仅显示在面板，不污染核心计算控制台输出。
- 清理遗留死数据 `cost_weight`（含物品额外成本每物品 4 字段）：从未被引擎/优化器读取、仅为重构前源项目占位；已从方案初始化移除，并在加载存档时自动清除，减小 localStorage 体积。

## 0.9.10 - 2026-08-21

- 结果区新增「其余目标值」板块：展示当前珍稀权重值、净热值（GJ），均带前后增减。
- 预估占地、多余产物增加增减展示（占地增减改为数值下方上下排布）。

## 0.9.9 - 2026-08-21

- 新增「增产剂自由等级」开关：自动优化时，生产增产剂 Mk.I/II/III 的配方可自由选择各级增产剂（≤ 最高等级），不受可选增产剂限制。
- 硅石默认配方改为直接获取（采矿），而非石矿配方。
- 测试文件集中到 `tests/` 目录，新增 `npm test` 脚本。

## 0.9.8 - 2026-08-21

- 新增「珍稀权重」优化策略，替代并移除「最小原矿瓶颈」；新增珍稀矿实用性修正（默认开启），刺笋结晶/金伯利矿石/分形硅石按可替代普通矿折算稀缺度（替代比例 95%）。
- 布局调整：可选增产剂行移到矿物可用量与添加需求之间，策略/自动优化按钮/可选增产剂行序重排。
- 默认深色主题、默认优化策略改为珍稀权重、无增产剂加权默认 0.1%。
- 燃料选择移除增产剂 Mk.I/II/III。
- 矿物可用量按钮在展开时高亮；切到非珍稀权重策略自动收回矿物可用量。
- 「已自动应用统计均值」提示仅在真正应用后显示 5 秒。
- 导航栏新增 star 文案链接。
- 全项目 eslint 清理，修复 hooks 依赖等代码问题。

## 0.9.7 - 2026-08-20

- 自动优化默认仅启用增产剂 Mk.III。
- 无增产剂加权默认值设为 0.5%。
- 同步优化器默认配置和相关文档。

## 0.9.6 - 2026-08-20

- 新增浏览器 WASM 种子查询，公开 GitHub Pages 版本默认无需 Python 后端。
- 保留本地 FastAPI 查询和统计后端，可通过控制台切换 `browser`、`backend` 或 `auto` 模式。
- 统计结果支持从公开 `public/stats.json` 读取；后端统计控制 UI 默认隐藏，可通过 `showStatsControls()` 显示。
- 内置 `dsp_search_seed` C++/Python 查询依赖，消除对项目外部绝对路径的依赖。
- 新增 WASM 构建脚本 `npm run build:wasm`，通过 `GLM_ROOT` 配置 GLM 路径。
- 统计计算使用 Welford online algorithm，维护方差、置信区间和相对误差，并以星区汇总指标判断收敛。
- 种子查看器支持 32–64 恒星数量、矿量/矿点应用，以及选择星区或指定恒星应用资源数据。
- 删除过时的 `docs/deploy.md`，并按当前实现重写 `docs/code-analysis.md`。
